spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/studentmanagement?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC'
    username: root
    password: zd004921
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted   # 实体类字段名
      logic-delete-value: 1         # 删除标记值
      logic-not-delete-value: 0     # 未删除标记值
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启SQL日志
  type-aliases-package: com.example.entity # 实体类包路径

server:
  port: 443  # HTTPS 默认端口
  ssl:
    key-store: classpath:keystore1.p12      # 证书路径（自签名）
    key-store-password: zd004921            # 密钥库密码
    key-store-type: PKCS12                 # 证书类型
    key-alias: localhost                    # 证书别名